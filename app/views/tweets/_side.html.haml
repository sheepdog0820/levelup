.said
  .said__main
    .said__main__stas
      .said__main__stas__image
        = image_tag current_user.image.url,size: "100x100"
      .said__main__stas__name
        = current_user.name
        %p Lv:100
    .said__main__tag
      = pie_chart @chart
  .said__content
    .said__content__box
      = link_to edit_user_path(current_user) do
        = icon('fas', 'home')
        マイページ
    .said__content__box
      = link_to destroy_user_session_path(current_user), method: :delete, class: "" do
        = icon('fas', 'door-open')
        ログアウト
    .said__content__box
      = link_to new_group_path do
        = icon('far', 'clipboard')
        グループ
    .said__content__box
      = icon('fas', 'tag')
      タグ検索
    .said__content__box
      = icon('fas', 'keyboard')
      キーワード検索